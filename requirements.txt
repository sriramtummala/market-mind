faiss-cpu==1.12.0
scikit-learn==1.8.0
numpy==2.5.1
fastapi==0.118.0
uvicorn==0.31.1
pydantic==2.12.0
requests==2.32.3
anthropic==0.40.0
python-dotenv==1.2.2
langgraph==0.2.28
streamlit==1.40.0

# Document intelligence (OCR + PDF processing)
pytesseract==0.3.13
pymupdf==1.25.0
pillow>=10.0.0
reportlab==4.2.5

# Multi-model providers + fine-tuning
openai>=1.40.0
boto3>=1.34.0

# LLMOps stack (MLflow works fully offline; the rest are
# optional real integrations with graceful local fallbacks if omitted)
mlflow==3.14.0
deepeval==4.1.0
langsmith==0.10.0
# trulens-eval     # optional — enables real groundedness feedback functions
# ragas            # NOT installed: every current ragas version pulls in an
                   # unpinned `langchain`, whose current release requires
                   # langgraph>=1.2.5 — a breaking major-version jump from our
                   # pinned langgraph==0.2.28 that src/agents/graph.py depends
                   # on. Re-evaluate once ragas decouples from full langchain,
                   # or isolate it in a separate eval-only venv.

# MCP tool server
mcp==1.28.1
