faiss-cpu==1.12.0
scikit-learn==1.8.0
numpy==2.5.1
fastapi==0.118.0
uvicorn==0.31.1
pydantic==2.12.0
requests==2.32.3
anthropic==0.34.2
langgraph==0.2.28
streamlit==1.40.0

# Day 8 — document intelligence (OCR + PDF processing)
pytesseract==0.3.13
pymupdf==1.25.0
pillow>=10.0.0
reportlab==4.2.5

# Day 10 — multi-model providers + fine-tuning
openai>=1.40.0
boto3>=1.34.0

# Day 11 — LLMOps stack (MLflow works fully offline; the rest are
# optional real integrations with graceful local fallbacks if omitted)
mlflow==3.14.0
# langsmith        # optional — enables real tracing if installed + API key set
# trulens-eval     # optional — enables real groundedness feedback functions
# deepeval         # optional — enables real LLM-judge test assertions
# ragas            # optional — enables real faithfulness/relevancy/precision metrics

# Day 12 — MCP tool server
mcp==1.28.1
